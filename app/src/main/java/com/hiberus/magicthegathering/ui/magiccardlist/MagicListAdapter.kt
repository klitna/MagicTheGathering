package com.hiberus.magicthegathering.ui.magiccardlist

import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.hiberus.magicthegathering.R

class MagicListAdapter {
    /*var cardList: List<MagicCardResult> = emptyList<MagicCardResult>()

    fun setData(list: List<MagicCardResult>) {
        cardList = list
        notifyDataSetChanged()
    }

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): SearchViewHolder {
        return SearchViewHolder(LayoutInflater.from(parent.context).inflate(R.layout.card_magic_search))
    }

    override fun getItemCount(): Int {
        return cardList.size
    }

    override fun onBindViewHolder(holder: SearchViewHolder, position: Int) {
        val card = cardList[position]
        holder.itemView.card

    }*/
}